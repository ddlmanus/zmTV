# 安全策略

## 报告安全问题

请通过 GitHub Security Advisory 的私密报告功能联系维护者。不要在公开 Issue 中披露漏洞细节、API Key、数据库连接、OSS 凭据或用户素材。

## 凭据处理

- 仓库不提供测试密钥。
- 桌面端密钥应保存在系统安全存储中。
- OSS Secret 和数据库凭据只能存在主进程环境或服务端。
- 发现凭据误提交后，应立即撤销并轮换；仅删除最新提交不足以清除 Git 历史。

## 支持范围

安全修复以当前默认分支为准。第三方模型、供应商接口和本地 AI 模型的安全问题应同时报告给其维护方。
