# 贡献指南

感谢参与 zmTV。提交改动前，请先确认用途符合非商业许可证。

## 本地检查

```bash
npm install
npx prettier --check <本次修改的文件>
npx tsc --noEmit
npm run build
```

## 代码约定

- 使用 TypeScript strict mode。
- 按功能拆分画布、节点、参数、供应商和任务轮询代码。
- UI 状态放入现有 Zustand store，避免在大型组件中重复维护。
- 模型参数从 schema 动态生成，不新增按模型名称写死的参数面板。
- 修改共享任务或输出结构时，同时检查图片、视频、音频、3D 和数字人。
- 不提交 `.env*`、数据库、日志、生成历史、用户素材或真实凭据。

## 提交说明

Pull Request 请写明：

- 解决的问题与影响范围。
- 使用的供应商和模型类型，但不要粘贴 API Key。
- 已运行的静态检查与构建命令。
- 涉及界面时附上桌面端截图和复现步骤。
- 是否执行过真实付费生成；未执行时要明确说明。
